# RAWx LAB — প্রোটোটাইপ ব্যবহারের নির্দেশিকা

এটি একটি **স্ট্যাটিক (no-backend) প্রোটোটাইপ**, যা Urbaland-এর প্রোডাক্ট ক্যাটাগরি স্ট্রাকচার ব্যবহার করে **"RAWx"** ব্র্যান্ড আইডেন্টিটির (brutalist / industrial leather-lab থিম) মধ্যে ফের ডিজাইন করা হয়েছে। কোনো সার্ভার, ডেটাবেস বা পেমেন্ট গেটওয়ে নেই — শুধু ডেমো/ভিজ্যুয়াল ফাংশনালিটি।

---

## ১. ফাইল স্ট্রাকচার

```
rawx-lab/
├── index.html      → হোমপেজ
├── shop.html        → পুরো রেজিস্ট্রি/ক্যাটালগ (ফিল্টার + সার্চ + সর্ট)
├── product.html      → একটা প্রোডাক্টের স্পেক-শীট পেজ (?id=... দিয়ে)
├── about.html         → ওয়ার্কশপ/ব্র্যান্ড স্টোরি
├── contact.html         → যোগাযোগ / ইনকোয়ারি ফর্ম (ডেমো)
├── style.css              → সব পেজের শেয়ারড CSS (একটাই ফাইল)
├── data.js                 → সব ক্যাটাগরি ও প্রোডাক্টের ডেটা (এখানেই এডিট করবেন)
├── app.js                    → নেভিগেশন, কার্ট, টোস্ট, স্লাইডার, কার্ড রেন্ডারিং লজিক
├── নির্দেশিকা.md              → এই ফাইল
└── assets/
    ├── hero/                 → হিরো স্লাইডারের ৬টা ছবি
    └── categories/           → হোমপেজ ক্যাটাগরি কার্ডের ছবি
```

CSS এবং JS ইচ্ছাকৃতভাবে HTML থেকে আলাদা ফাইলে রাখা হয়েছে, যাতে ডিজাইন (style.css) আর লজিক (data.js / app.js) আলাদাভাবে মেইনটেইন করা যায়।

---

## ২. কীভাবে চালাবেন

সবচেয়ে সহজ উপায় — যেকোনো একটা লোকাল সার্ভার দিয়ে চালান (`file://` দিয়ে সরাসরি খুললে fetch/route কিছু জায়গায় সমস্যা করতে পারে):

**Python থাকলে:**
```bash
cd rawx-lab
python3 -m http.server 8000
```
তারপর ব্রাউজারে যান: `http://localhost:8000`

**VS Code ব্যবহার করলে:** "Live Server" এক্সটেনশন দিয়ে `index.html` ওপেন করুন।

**কোনো সার্ভার না থাকলে:** সরাসরি `index.html` ডাবল-ক্লিক করেও প্রায় সবকিছু কাজ করবে, কারণ সব ডেটা `data.js`-এ লোকাল ভাবে সংজ্ঞায়িত (কোনো external API কল নেই)।

---

## ৩. পেজ-বাই-পেজ কী আছে

- **index.html** — ৬-স্লাইড হিরো ইমেজ স্লাইডার (অটোপ্লে + ডট + অ্যারো নেভিগেশন), ১০টা ক্যাটাগরির ফটো-ব্যাকড পিলার-স্ট্রিপ, এবং "Recently Tagged" নামে ৮টা ফিচারড প্রোডাক্ট।
- **shop.html** — সব প্রোডাক্ট এক জায়গায়। উপরে ক্যাটাগরি চিপ (বাটন), সার্চ বক্স, আর সর্ট ড্রপডাউন। URL-এ `?cat=বিফোল্ড-আইডি` দিলে সেই ক্যাটাগরি প্রি-ফিল্টার হয়ে খুলবে (যেমন `shop.html?cat=belt`)।
- **product.html** — `?id=প্রোডাক্ট-আইডি` প্যারামিটার দিয়ে একটা প্রোডাক্টের ডিটেইল/স্পেক-শীট দেখায় (batch code, ম্যাটেরিয়াল, কালারওয়ে, দাম) + একই ক্যাটাগরির রিলেটেড প্রোডাক্ট।
- **about.html** — ব্র্যান্ড স্টোরি, ৩টা কোর ভ্যালু, আর ৪-স্টেজ প্রসেস টাইমলাইন (Source → Cut → Stitch → Register)।
- **contact.html** — কন্টাক্ট ইনফো + একটা ডেমো ইনকোয়ারি ফর্ম (সাবমিট করলে শুধু একটা toast মেসেজ দেখাবে, কোথাও ডেটা পাঠানো হয় না)।

---

## ৪. নতুন প্রোডাক্ট / ক্যাটাগরি যোগ করবেন কীভাবে

সব ডেটা **`data.js`** ফাইলে। HTML/CSS ছোঁয়ার দরকার নেই।

**নতুন ক্যাটাগরি যোগ করতে**, `RAWX_CATEGORIES` অ্যারেতে একটা নতুন অবজেক্ট যোগ করুন:
```js
{ id: "wallets-mini", code: "11", name: "Mini Wallet", tagline: "Pocket-size / coin pouch" }
```

**নতুন প্রোডাক্ট যোগ করতে**, `RAWX_PRODUCTS` অ্যারেতে `rxItem(...)` কল করুন:
```js
rxItem("bifold", "New Product Name", 899, "Full-grain cowhide", "Black", "Dhaka, BD")
```
প্যারামিটার অর্ডার: `ক্যাটাগরি-আইডি, নাম, দাম (৳), ম্যাটেরিয়াল, কালারওয়ে, অরিজিন`।

Batch code (`RX-XX-XXX`) অটোমেটিক জেনারেট হয় — কিছু ম্যানুয়ালি করার দরকার নেই।

---

## ৫. ছবি আপলোড করবেন কীভাবে — `assets/` ফোল্ডার

সাইটে এখন সত্যিকারের ফটো বসানোর জন্য একটা রেডি ফোল্ডার স্ট্রাকচার আছে। এখন যা আছে সেগুলো **প্লেসহোল্ডার ছবি** (RAWx LAB ব্র্যান্ড-কালারে জেনারেট করা) — আসল প্রোডাক্ট ফটো দিয়ে **একই নামে ফাইল রিপ্লেস করলেই** কাজ হয়ে যাবে, কোনো কোড বদলাতে হবে না।

```
rawx-lab/
└── assets/
    ├── hero/                  → হোমপেজ হিরো স্লাইডার (৬টা স্লাইড)
    │   ├── slide-1.jpg          (Signature Collection)
    │   ├── slide-2.jpg          (Bifold & Trifold)
    │   ├── slide-3.jpg          (Long Wallet & Card Holder)
    │   ├── slide-4.jpg          (Bag & Backpack)
    │   ├── slide-5.jpg          (Ladies Line)
    │   └── slide-6.jpg          (Trekon Utility)
    └── categories/             → হোমপেজে ক্যাটাগরি কার্ড (১০টা)
        ├── bifold.jpg
        ├── trifold.jpg
        ├── longwallet.jpg
        ├── belt.jpg
        ├── bag.jpg
        ├── cardholder.jpg
        ├── passportholder.jpg
        ├── accessories.jpg
        ├── ladiesbag.jpg
        └── trekon.jpg
```

### রিকমেন্ডেড সাইজ
| ফোল্ডার | সাইজ (px) | অনুপাত | ফরম্যাট |
|---|---|---|---|
| `assets/hero/` | ১৯২০ × ১০৮০ | 16:9 (landscape) | .jpg |
| `assets/categories/` | ৯০০ × ১১২৫ | 4:5 (portrait) | .jpg |

### কীভাবে বদলাবেন
1. আপনার আসল ফটো রিসাইজ করে উপরের সাইজে নিয়ে আসুন।
2. ঠিক **একই ফাইলনেম** দিয়ে (যেমন `slide-1.jpg`, `bifold.jpg`) সেভ করুন।
3. `assets/hero/` বা `assets/categories/` ফোল্ডারে পুরনো ফাইলের জায়গায় নতুন ফাইল বসিয়ে দিন (ওভাররাইট করুন)।
4. পেজ রিফ্রেশ করলেই নতুন ছবি দেখা যাবে — HTML/CSS/JS কিছু বদলাতে হবে না।

### ফাইলনেম বদলাতে চাইলে বা নতুন ক্যাটাগরি/স্লাইড যোগ করতে চাইলে
- হিরো স্লাইডের কন্টেন্ট (ছবি পাথ, টাইটেল, ট্যাগলাইন) থাকে `data.js`-এর `RAWX_HERO_SLIDES` অ্যারেতে।
- ক্যাটাগরি কার্ডের ছবি পাথ থাকে `data.js`-এর `RAWX_CATEGORIES` অ্যারের প্রতিটা অবজেক্টে `image:` ফিল্ডে।
- সেখানে পাথ বদলালেই যেকোনো নতুন ফাইলনেম/ফোল্ডার ব্যবহার করতে পারবেন।

### ছবি না থাকলে কী হয়?
যদি কোনো ছবি ফাইল মিসিং থাকে বা লোড হতে ফেইল করে, সাইট **ভাঙা ইমেজ আইকন দেখাবে না** — বরং অটোমেটিক্যালি ব্র্যান্ড-কালারের টেক্সচার প্যাটার্নে ফলব্যাক করবে (`rawxImgFail()` ফাংশন, `app.js`-এ)। তাই ধাপে ধাপে ছবি আপলোড করলেও সাইট সবসময় প্রেজেন্টেবল থাকবে।

---

## ৬. ব্র্যান্ডিং / ডিজাইন টোকেন (style.css-এর `:root`-এ)

| ভ্যারিয়েবল | মানে | কালার |
|---|---|---|
| `--bg` | মূল ব্যাকগ্রাউন্ড (raw charcoal) | `#0D0C0A` |
| `--tan` | মেইন ব্র্যান্ড অ্যাকসেন্ট (saddle tan) | `#C1793D` |
| `--stitch` | সেকেন্ডারি অ্যাকসেন্ট (oxblood thread) | `#B5452F` |
| `--paper` | মূল টেক্সট কালার (hide parchment) | `#EDE6D6` |

রং পরিবর্তন করতে চাইলে শুধু এই `:root` ব্লকের ভ্যারিয়েবলগুলো বদলালেই পুরো সাইটের থিম বদলে যাবে।

ফন্ট: **Barlow Condensed** (heading/লোগো/টাইটেল) + **Space Mono** (batch code, লেবেল, ছোট টেক্সট) — এই দুইটা Google Fonts CDN থেকে লোড হয়।

---

## ৭. যা এই প্রোটোটাইপে নেই (real store বানাতে হলে যোগ করতে হবে)

- আসল পেমেন্ট/checkout সিস্টেম
- প্রোডাক্ট কার্ড ও স্পেক-শীটে এখনো CSS-জেনারেটেড টেক্সচার swatch ব্যবহার হয় (শুধু হিরো স্লাইডার আর হোমপেজ ক্যাটাগরি কার্ডে আসল ছবি বসানো যায় — চাইলে পরের ধাপে প্রতিটা প্রোডাক্টেও `image:` ফিল্ড যোগ করে একই সিস্টেম চালু করা যাবে)
- ইউজার লগইন / অর্ডার হিস্ট্রি
- সার্ভার-সাইড ডেটাবেস (এখন সব ডেটা `data.js`-এ হার্ডকোড করা)
- ফর্ম সাবমিশন কোনো ইমেইল/API-তে যায় না

এই প্রোটোটাইপটা মূলত **ভিজ্যুয়াল ডিরেকশন ও ইনফরমেশন আর্কিটেকচার** দেখানোর জন্য — অ্যাপ্রুভালের পর Urbaland-এর আসল প্রোডাক্ট ছবি/দাম/স্টক ডেটা দিয়ে রিপ্লেস করে একটা রিয়েল ব্যাকএন্ডের (e.g. getCommerce, Shopify, বা কাস্টম) সাথে কানেক্ট করা যাবে।
